package org.lsmr.selfcheckout.devices.listeners;

import org.lsmr.selfcheckout.devices.AbstractDevice;

/**
 * Represents listeners that respond only to enable/disable events on touch screens.
 */
public interface TouchScreenListener extends AbstractDeviceListener {
}
